;; My passwords are obviously not here. These are for you to override yourself.

(add-to-list 'load-path "~/.emacs.private")
(load "weblogger-config")

;(load "real-passwords" 'noerror)
;(load "wp-list")
